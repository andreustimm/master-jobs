# Codex configuration for master-jobs.
#
# Instructions live in AGENTS.md at the repository root — the single authored
# source. CLAUDE.md is a symlink to it, so there is only one file to edit.
# Rule details live in docs/engineering/rules/; this file only wires Codex up.

[project]
name = "master-jobs"
instructions = "AGENTS.md"

# Every shell command must be prefixed with `rtk` in Codex (see AGENTS.md).
# The commands below are the ones worth knowing before touching anything.
[project.commands]
install = "rtk pnpm install"
test = "rtk pnpm test"
typecheck = "rtk pnpm typecheck"
check = "rtk pnpm check"
sync = "rtk pnpm jho jobs sync"
list = "rtk pnpm jho jobs list --min-fit 55"
pipeline = "rtk pnpm jho pipeline"
