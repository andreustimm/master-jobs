# Notas posteriores ao arquivamento

Os artefatos `_spec.md`, `_tasks.md` e `_tests.md` foram preservados byte a
byte para que o diff registre a movimentação histórica como `R100`. Por isso,
duas referências daquele retrato continuam descrevendo a localização anterior:

- a ADR relacionada é a
  [ADR 0011](../../../../docs/adr/0011-fronteira-compozyos-e-docs.md);
- `T1`–`T4` e `T6`–`T9` estão em `tests/rate-limit.test.ts`; `T5` e `T10`
  são comprovados pela jornada real em `tests/e2e/ui.mjs`.

O manifesto `_tasks.v2.md` e os arquivos `task_*.md` ao lado registram a
conversão usada pelo Compozy para validar e arquivar a feature já entregue.
